{
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "framework": "vite",
    "functions": {
        "api/setup.ts": {
            "includeFiles": "db/**"
        }
    },
    "rewrites": [
        { "source": "/((?!api/).*)", "destination": "/index.html" }
    ]
}
